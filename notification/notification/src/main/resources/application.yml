#spring.application.name=notification

spring:
  application:
    name: notification
  rabbitmq:
    host: localhost
    port: 5672
    username: mozammil
    password: mozammil

server:
  port:
    8084

rabbitmq:
  exchange:
    name: order.exchange
  queue:
    name: order.queue
  routing:
    key: order.tracking