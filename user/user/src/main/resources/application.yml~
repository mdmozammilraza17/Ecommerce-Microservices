spring:
  application:
    name: user
  data:
    mongodb:
      uri: mongodb://mongo_user:mongo_pass@localhost:27017/ecom_user?authSource=admin
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

server:
  port: 8085
